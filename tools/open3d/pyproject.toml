[build-system]
requires = ["setuptools>=77", "wheel"]
build-backend = "macos_build_backend"
backend-path = ["."]

[tool.uv]
cache-keys = [{ file = "pyproject.toml" }, { file = "macos_build_backend.py" }, { file = "*.patch" }]
